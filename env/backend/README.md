O modelo de variáveis para desenvolvimento **com Node local** está em **`backend/.env.example`**.

Na pasta `backend`, copie para `.env.development` (e opcionalmente `.env.test` a partir de `backend/.env.test.example`).
